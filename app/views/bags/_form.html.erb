
<%= simple_form_for [@bag] do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.select :bag_type, ['PET', 'TIN', 'Mixed/Dry'] %>
  </div>
  <br>
  <div class="form-actions">
    <%= f.button :submit, class: 'btn btn-success' %>
  </div>
<% end %>
